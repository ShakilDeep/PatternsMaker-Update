# Remaining-gap audit — 2026-09-10

Outcome: gaps remain. This audit checks the current worktree; it does not implement the fixes or declare the product complete.

## Reproduced defects

The five isolated API probes in `test_gap_audit.py` all fail their intended-behavior assertions. They use temporary SQLite databases and supplied demo inputs. Exact observations are saved in `gap-audit-results.json`.

| Priority | Defect | Observed behavior | Source |
|---|---|---|---|
| High | Measurement-specific PATCH ignores route scope and bypasses range checks | PATCH to `/measurements/half_chest` with `sleeve_length: -3` returns 200 and saves −3 cm to the sleeve | `backend/app/api/routes.py:123`; `application/service.py:100` |
| High | Archived workbook remains eligible for generation | After archiving the workbook, requirements still report ready and generation returns 200 | `backend/app/api/lifecycle_routes.py:26`; `application/requirements.py` |
| High | JSON import accepts invalid nested geometry | A valid base pattern with an empty grade and malformed marker returns 200 with those objects | `backend/app/api/artifact_routes.py:37`; `api/schemas.py` |
| Medium | Import ignores project existence | The same valid import posted to `/projects/nonexistent/exports/import` returns 200 | `backend/app/api/artifact_routes.py:37` |
| High | Version-scoped grading uses current settings | Requesting grading of an older zero-allowance version after generating a 2 cm version produces 2 cm grades | `backend/app/api/routes.py:178`; `application/service.py:180` |

Reproduce from `backend` with the patter-codex interpreter:

```bash
python -m pytest ../artifacts/qa/test_gap_audit.py -c pyproject.toml -q --tb=short
```

Expected current result: **5 failed**. These probes are intentionally outside the normal regression suite; the previously passing 137 tests do not cover these defects. Promote them into the regular suite when fixing their behavior.

## Full remaining-area reconciliation

| miss.md area | Current assessment and direct evidence |
|---|---|
| 1. Visual acceptance | FAIL. `visual-comparison.json` and retained `reference-check` screenshots show 119,276 differing pixels at threshold 0.2 with zero allowed pixels. Source values/geometry also differ from the illustration; do not invent dimensions to hide that. |
| 2. Accessibility | Automated workflow and assistant scans exist. Manual screen-reader, comprehensive expanded/mobile/touch states and navigation-loss protection remain unverified. `Measurements.tsx` retains drafts and locks units/size while dirty, but has no general navigation guard. |
| 3. Release rehearsal | `release-readiness.md` still lists clean-machine setup and customer rehearsal as OPEN. The supplied evidence does not satisfy final release gates. |
| 4. Drafting | `domain/drafting.py` still uses simplified demo formulas; forward-shoulder values and full sleeve construction placement are not implemented. Approved production blocks/rules, pocket dimensions and physical fit remain external. |
| 5. Validation | Six-size goldens and several tolerance boundaries now exist. Angle/flattening/intersection policies and construction placement remain incomplete. `validation.py` has registered validators and grading decrease warnings; blanket claims that these are absent are stale. |
| 6. Source lifecycle | Archive/restore/replacement/compare routes exist, but archival does not enforce readiness (reproduced). Compare returns histories rather than field-level differences. `parsers.py` caps file size, page count and extracted text; it has no comparable pre-extraction PDF resource/decompression guard. |
| 7. Requirements | `requirements.py` does not gate archived sources or low-confidence cells. Calibration evidence is persisted, but marker/export policies are not all first-class requirements. `Workflow.tsx` renders a list rather than a guided, filterable queue. |
| 8. API | Routes and generic Pydantic object/list responses exist, correcting the old “missing routes” claim. Typed domain contracts, pagination and actual version/measurement scope enforcement remain incomplete; see reproduced failures. |
| 9. Export/import | Base geometry is validated, but grades/marker and target project are not (reproduced). Import returns a payload without persisting a restored project. SVG lacks a complete typed mark schema; PDFs explicitly remain preview reports. Certified production formats are deferred. |
| 10. Marker | `marker.py` performs one deterministic first-fit pass. Seed, iteration count and budget are echoed; there is no search loop, clock/deadline enforcement, progress or cancellation. `MarkerPanel.tsx` displays previous metrics, not a visual layout comparison. |
| 11. Assistant | Context, validation, malformed-provider tests and frontend selection wiring now exist. Broad semantic mapping with structured acceptance and extraction-provider integration remain open. Static inspection also shows base-pattern-first selection in `assistant_context.py` versus grade-first selection in `App.tsx` when both hold the same size; this needs a dedicated version-consistency test. |
| 12. Operations | Local security baseline exists; public auth is deferred. Rate/resource controls, retention/threat-model evidence, full parser/grading/frontend benchmarks and release vulnerability evidence remain absent or unverified. Assistant error redaction is covered, but that does not prove all unexpected-error logs are safe. |
| 13. Architecture | `validation.py` already has registered validator objects. Remaining issues include loose dictionaries, generic response wrappers, route-level persistence and compressed frontend modules. |
| 14. Studio | `StudioControls.tsx` supports only current-base allowance/notch/fold/history operations; ruler/grid/snapping/layers, dimension overlays, before/after preview and a full provenance/seam inspector remain missing. |
| 15. Dashboard/lifecycle | Archive action and restore API exist. `ProjectDashboard.tsx` has no restore control, archived-project filtering or guided onboarding/handoff. Historical claim that archive is wholly absent is stale. |
| 16. Review/export UX | `ReviewPanel` remains under Source Review. `Workflow.tsx` export cards lack per-selected-artifact readiness, inline approval/rejection and preview. Controls use the base pattern even when a different size is selected. |

## Verification limits

The latest recorded broad regression is 137 backend tests, 19 frontend tests, build/lint/types passing. Those suites were run in the immediately preceding implementation session; this audit additionally ran five targeted negative-path probes. No new broad-suite pass is claimed here. The new failures contradict release completeness despite the earlier green regression result.

Priority: fix the reproduced API/data-integrity defects, then complete artifact validation/readiness, lifecycle and version consistency. Keep the strict visual gate and manual/rehearsal gates open. Production certification, real 3D, certified DXF, OCR and public multi-user deployment remain outside the current demonstrated local scope.
